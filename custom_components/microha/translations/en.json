{
  "config": {
    "step": {
      "user": {
        "title": "Set up MicroHA Gateway",
        "description": "Do you want to set up the MicroHA Gateway for your microcontrollers?"
      }
    },
    "abort": {
      "single_instance_allowed": "MicroHA Gateway is already configured. Only a single instance is needed."
    }
  }
}
